plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.educationapp"
    compileSdk = 36
    ndkVersion = "26.2.11394342"
 // ✅ Hardcoded instead of flutter.compileSdkVersion

    compileOptions {
        coreLibraryDesugaringEnabled true
        sourceCompatibility = JavaVersion.VERSION_17 // ✅ Upgraded to Java 17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17" // ✅ Upgraded Kotlin JVM target
    }

    defaultConfig {
        applicationId = "com.example.educationapp"
        minSdkVersion = flutter.minSdkVersion
        targetSdk = 33

        versionCode = 1 // ✅ Replace with actual versionCode
        versionName = "1.0" // ✅ Replace with actual versionName
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
            minifyEnabled true
            shrinkResources true
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation 'androidx.core:core-ktx:1.9.0'  // ✅ Ensure core-ktx is present
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.0.3' // ✅ Added required dependency
}
